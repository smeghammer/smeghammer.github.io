These files patch DOOM v1.1 Registered to DOOM v1.2 Registered.  To
use, unzip these files into the DOOM directory and enter PATCH at
the command line. 

When the job is done you can go back and delete the follwing patch
files.  PATCH.*, FILE_ID.DIZ and README.PAT.
